services:
  - type: web
    name: farmer-market-platform
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    healthCheckPath: /api/bootstrap
